import AdminLogin from "@/components/Admin/AdminLogin/AdminLogin";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AdminLogin />
    </div>
  );
};

export default page;
